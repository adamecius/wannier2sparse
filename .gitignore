# Build directories
/build/
build*/
cmake-build-*/

# CMake-generated files (if a build is ever configured in-tree)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
install_manifest.txt
DartConfiguration.tcl
Testing/

# Generated version header (configured by CMake, not source)
include/Version.h

# Compiled objects / libraries / binaries
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll

# Tool output (sparse matrices produced by wannier2sparse)
*.CSR

# Editor / OS clutter
.vscode/
.idea/
*.swp
*~
.DS_Store

# Local-only reference PDFs (only the manifest agent_references.md is committed)
agent_references/
